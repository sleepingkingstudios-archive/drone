-# app/views/roles/index.html.haml

%h1 Roles

%table
  %thead
    %tr
      %td Company
      %td Title
  %tbody
    - if @roles.blank?
      %tr
        %td{ :colspan => 2 } There are no roles to view.
    - @roles.each do |role|
      %tr
        %td= role.company
        %td= role.title
